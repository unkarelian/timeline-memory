
// boy i'm tired of copy/pasting this whole block every time
export const log = (...msg)=>console.log('[timeline-memory]', ...msg);
export const debug = (...msg)=>console.debug('[timeline-memory]', ...msg);
export const error = (...msg)=>console.error('[timeline-memory]', ...msg);
